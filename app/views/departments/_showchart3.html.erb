<div class="well">
	<script type="text/javascript" charset="utf-8">
		$(function() {
			var _dept,_url,_url2
			_dept = <%= @department.id %>
			_url = '/departments/' + _dept + '/deptindices/1005/dept_index.json'
			_url2 = '/departments/' + _dept + '/deptindices/1006/dept_index.json'
			_url3 = '/departments/' + _dept + '/deptindices/1007/dept_index.json'

		  return $.get(_url, function(data_1) {
				return $.get(_url2, function(data_2) {
					return $.get(_url3, function(data_3) {
						var chart, _month
								// _month = [];
								// data_1.map(function(y) {
								// _month.push(y.month);
								// })
						// alert (data_2[0].total)
						new Highcharts.Chart({
							// colors: ['#AA4643','#4572A7'],
							chart: {
								renderTo: "chart_3",
								plotBackgroundColor: null,
								plotBorderWidth: null,
                plotShadow: false
							},
							title: {
								text: "营销团队发展情况"
							},
	            tooltip: {
	              formatter: function() {
							  	return '<b>'+ this.point.name +'</b>: '+ this.percentage +' %';
								}
	            },
	            plotOptions: {
	                pie: {
	                    allowPointSelect: true,
	                    cursor: 'pointer',
	                    dataLabels: {
	                        enabled: true,
	                        color: '#000000',
	                        connectorColor: '#000000',
	                        formatter: function() {
	                            return '<b>'+ this.point.name +'</b>: '+ this.percentage +' %';
	                        }
	                    }
	                }
	            },
							credits: {
								enabled: false
							},
							series: [{
								type: 'pie',
								data: [
		          		{ name: 'A', y: data_2[0].total },
				          { name: 'b', y: data_3[0].total },
		            ]
							}]
						})
			   });
			  });
		  });
		 });
	</script>
	<div id="chart_3">
	</div>
</div>